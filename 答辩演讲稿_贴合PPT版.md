# 决赛答辩演讲稿（贴合25页PPT · 润色版）

> 正文约12分钟，可压到10分钟（【可删】处约1.2分钟）。
> 页码对应PPT，方便对稿翻页。加粗处是**必须念准的数字**，其余按口语自然讲。

---

## 封面（P1）

各位老师好。我是北航穹卫反无集群协同规划组的常耀宇，今天代表团队汇报
《面向低空反无的分层高效集群动态拦截》。团队还有闵晨、雷阳琦、张宇珺，
指导老师是全权教授。

---

## Part 1 · 研究背景与任务需求（P3–P5，约1.5分钟）

**【P4】**

这几年低空空域变得越来越拥挤。一边是物流、巡检这些合作式应用在快速铺开，
另一边，近几年几场地区冲突已经反复说明一件事——低成本无人机集群，
正在成为一种规模化的威胁手段。

问题在于成本账算不过来。你用一枚导弹去拦一架几千块的无人机，
拦得越多亏得越多。所以反无人机这件事，最后一定会走向"用集群对抗集群"。

而要指挥集群，就得先有能指挥集群的规划算法。这是我们这项工作的出发点。

**【P5】**

我们梳理了目前几个有代表性的方案，MADER、EGO-Swarm、Primitive-Swarm。
它们在各自的设定下都做得很好。但本场景的难点在于四个条件是**同时**出现的：
大规模集群、密集障碍物、动态目标点与动态障碍物、还要实时。

任何一条单独拿出来都不算新问题。四条叠在一起，情况就完全不一样了。

---

## Part 2 · 初赛静态规划（P6–P10，约2分钟）

**【P7】**

先简单回顾初赛。场景是纯静态的：24架无人机从原点附近起飞，
横向铺开在正负20米以内，穿过一片障碍区，飞向7公里外的24个拦截点，
一对一完成分配。障碍是一个圆柱加两个长方体，连成一段需要连续绕行的通道。

**【P8】**

方案选型上，主流路线大致三类。采样类，像RRT星、PRM，高维适应性好，
但每次跑出来的结果不一样，路径也不够平滑；搜索类，A星、D星Lite，
结果稳定、可复现，代价是分辨率一高计算量就上去；还有优化和势场类，
能直接把运动约束写进去，但容易陷进局部解，规模一大就扩展不动。

我们最后用的是一条五级流水线——解析环境建模、分层拓扑搜索、任务指派与分流、
B样条联合优化，最后做时间参数化和解冲突。

这里想特别点一下第三级，任务指派与分流。这一级是专门为"集群"设的，
它做的事情是优化目标匹配、减少轨迹交叉。当时我们的体会是：
**24条各自最优的路径，放在一起不一定可行**。这句话在决赛被彻底验证了。

**【P9–P10】**

初赛的结果：集群完工 **237.58秒**，最长单机航程7.1公里。
在线规划总计 **203.02毫秒**，单机热启动8.6毫秒。

约束这边全部达标，而且留了余量——最大速度刚好卡在30，最大高度77.92米、
限制是100；离障距离最小27.88米，要求是12米；机间最小13.17米，要求10米。
我们还按大约1米的间隔，对24条轨迹逐点复核了离障安全和端点。

初赛结束时我们总结了两条，直接决定了决赛的技术路线。
一是可复现性对竞赛来说非常关键，随机性强的方法，评委复跑时就是风险。
二是刚才那句，单条路径的最优对集群没有意义。

---

## Part 3 · 决赛动态规划方案升级（P11–P18，约4.5分钟，重点）

**【P12】**

决赛把三样东西同时变成了动态的。

先看约束怎么变的。速度上限从30放宽到50，听起来是松了。但安全距离反过来收紧了：
机间从10米压到 **3米**，离障从12米压到 **1.5米**。裕度砍掉了七成。

障碍环境是16个静态加3个动态。最关键的变化在任务本身——
飞到3500米触发目标切换，终点重组成三组，而且目标点本身还在抖动。

**【P13】**

这一页是三个升级维度。我想借这页澄清一个容易误读的数字。

大家会看到初赛写的是203毫秒，决赛写的是5.36秒，中间差了二十多倍。
但这两个数不是一回事。

初赛那个203毫秒，是**在线滚动规划**一轮的耗时。决赛的5.36秒，
是一次性把24条完整全局路径全都算出来——这是组委会明确定义的"单次规划时长"，
是个离线的一次性动作。

真正同口径的数字是：决赛的滚动周期耗时是 **177毫秒**，比初赛的203毫秒还快一点，
而且还多处理了动态障碍物预测和动态目标跟踪。

**【P14–P15】**

我们的系统分三层，分层的依据是**时间尺度**，不是功能模块。

L1全局引导层，整个任务只跑一次；L2局部轨迹规划层，每0.2秒一轮；
L3集群协调执行层，同样0.2秒。合起来是一个
"全局引导—局部生成—安全筛选—集群协调—同步执行"的滚动闭环。

为什么要分层？因为实时性、硬安全保证、可复现这三件事，
我们没找到哪一种单一方法能同时给到。分层之后，每一层只管它擅长的那一段。

**【P16】**

接下来这页是L1，也是我们这次收获最大的一页。

底子是稀疏网格上的加权A星，工程上做了两处加速。一处是把邻域里每条边
能不能走提前算好、存成查找表，搜索的时候只查表不算几何；
另一处是拿真实障碍几何做视线剪枝，把A星输出的锯齿路径拉直。

但真正重要的改动，是我们被一个奇怪现象逼出来的。

当时的情况是这样：同一套算法，同一个场景，只是输入坐标的精度差了几毫米，
结果就从成功翻成了失败。第一反应当然是数值问题，加个容差就完了。
但我们多问了一句为什么。

查下去发现，L1的代价函数是纯欧氏距离，它**根本不知道天上还有另外23架飞机**。
24架机起点终点都差不多，独立算出来的24条航路几乎是叠在一起的——
机间距只有 **3.478米**，而红线是3米。

也就是说，这是一个零裕度的系统。它不是"差一点点会出事"，
而是任何一点扰动都会把它推过线。

想明白这一点，问题的性质就变了——不是调参数，是L1这一层的设计缺了一块。
容差补不上这个缺口，必须在空域层面把飞机分开。

所以有了V2的空域分层。两件事：

一是高度分层，24架机交错分配到不同高度层。层间距不是试出来的，是推出来的——
它必须盖住L2局部垂直修正的范围，否则下层的避让动作会穿到隔壁层去。
实测下来，**跨层冲突是0次**。

二是横向走廊，在巡航段叠加多档横向偏移，每一档都拿真实障碍几何逐段校验。
走不通就自动缩小，再不行就退回去。它不是写死的一条路线，
而是一个会自己降级的阶梯。

**【P17】**

L2这一层，标题写的是"非反应式，而是预测式"，我展开说一下这个区别。

我们用五次多项式生成候选轨迹。选它是因为它能解析地满足位置、速度、加速度
三组边界条件，生成出来天然就是连续可导的，不需要再做平滑。
候选依次过三关：动力学约束、静态障碍、动态障碍。

关键在第三关。那3个动态障碍物是旋转长方体，它不光在平移，自己还在转。
如果按它当前位置去判断碰撞，等你飞过去的时候它早就不在那儿了。

所以我们做的是：预测它在未来整个时域内，每一个时刻的中心位置和偏航角，
然后逐时刻去校验候选轨迹。说白了，**我们躲的是它几秒后会在哪，
不是它现在在哪**。

**【P18】**

L3解决的是"谁让谁"。

传统的优先级规划给每架机一个固定优先级，这会有个后果：
排在后面的机可能一直被前面的挤掉，永远轮不上。

我们的做法是每个周期重新排一次。排序依据是候选裕度、冲突度和安全裕度——
简单说就是**谁最没得选，谁先选**。一架机如果被环境挤得只剩两三条可行轨迹了，
那它必须先挑，不然它那两三条也会被别人预约走。

另外还有一个机制叫一步前瞻，是专门防死锁的。一条轨迹此刻安全，
不代表下一步还走得动，它可能把飞机送进一个死胡同。所以我们会做一次影子推演：
假设就选它，推到下一周期还有没有可行解？没有就直接否掉。

还有一点，大规模机间避障我们没有做全局联合优化——那个规模下算不动。
我们把组合搜索限制在局部冲突簇里，让复杂度不随集群规模快速膨胀。

---

## Part 4 · 仿真验证（P19–P22，约2.5分钟）

**【P20】**

结果这边：24架全部抵达，任务时长 **116.60秒**，零碰撞，
集群总能耗626.920，路径平滑性1.149961。24条轨迹都通过了独立的安全复核。

**【P21】**

这一页是评分分析。要说明的是，题面只给了五项指标的权重，
没有给满分和零分对应的数值，所以归一化方法是我们自己定义的——
对"越小越好"的指标做线性插值再双端截断，然后按30、30、20、10、10加权。

请各位老师重点看路径安全这一行：最小机间距 **3.011米**，
最小静态障碍物距离2.355米，最小动态障碍物距离102.336米，全部达标，零违规。

平滑性这一项容易读反，我说明一下：它是曲率平方沿路径的积分，
直线的曲率是零，所以**数值越小，路径越平滑**。

还有一点我想坦率讲。这些基准是工程假设，不是官方数值。
比如规划时长，我们把满分线设在接近实时的1秒——这是个对自己挺苛刻的标准。
我们在代码注释里把"哪些是官方口径、哪些是我们的假设"逐条标注了，
评委如果有更合适的基准，只要改一个常量，评分逻辑一行都不用动。

**【P22】**

最后是接口这块。按组委会的模块化封装要求，我们把全部输入外置成了五类CSV，
障碍物、动态运动参数、起点、初始终点、切换后终点。
规划脚本和评分脚本是完全解耦的——评分脚本不import任何规划代码，只读JSON。

更重要的是，我们没有只在官方那一组数据上验证。我们自己造了几组变换场景，
主动去找方案会在哪儿垮掉：目标重排、起点压缩、新增障碍、目标扩展。

这些测试确实找出了真问题。举一个：起点压缩那组失败了，
但它不是代码的毛病——题面给的起点间距3.478米，本来就只剩3米红线的116%，
压缩一半之后飞机还没起飞就已经违规，被起飞前的校验直接拦下了。
**这说明起点密度已经没有压缩空间了，是题面数据本身的边界。**

【可删】另外还有一组新增障碍物的测试，失败点经过诊断，
精确落在了终点切换区域，而不是新增障碍物附近。这个反直觉的结果让我们意识到，
真正的拥堵源头是"24架机同时扇形散开"这个动作本身，
而不是航线上某一处几何窄道。

【可删】我们后来还拿组委会给的高速动态障碍物参数做了压力测试，
最快的障碍物速度是无人机的6倍多。第一次没跑通，诊断清楚之后做了针对性改进，
现在这组参数也能完成24架全部抵达。这部分细节我留到提问环节展开。

---

## Part 5 · 创新点与总结（P23–P25，约1分钟）

**【P24】**

四项创新对应我们的三层架构。

结构化空域分区规划，核心是把多机冲突的消解从"在线避让"前移到"离线空域预分配"；
分层滚动运动基元，是预测式而不是反应式的筛选；
轻量化集群时空协同，用动态优先级破解优先级饥饿，用一步前瞻破解集群死锁；
动态任务闭环规划，负责目标切换后的全局重规划、目标抖动的滤波，
以及终端的减速捕获。

**【P25】**

最后说一句。这次做下来，我们自己觉得最有价值的，其实不是某一组跑通的参数。

是我们逼着自己养成了一个习惯：每一次改动，都配一套能复现的量化诊断，
要能说清楚是第几个周期失败、哪两架机冲突、冲突发生在哪个坐标。
每一个假设，都拿实测去证伪——包括推翻我们自己提出来的假设。

有些结论就是这样被推翻的。这个过程不好看，但我们觉得比一次侥幸跑通更有意义。

汇报完毕，恳请各位老师批评指正。谢谢。

---
---

# 问答准备（5分钟）

## 一、方法选型

**Q1：为什么不直接用CBS或者ORCA这些成熟方案？**

CBS的完备性很好，但24机规模下冲突树会指数膨胀，实时性没法保证。
ORCA快，可它是纯反应式的，只看当下不看未来，而且在对称场景里特别容易死锁——
我们这个题目起终点高度对称，恰好就是它的最坏情况。

我们的L3其实是取了两者之长：用动态优先级保证实时性，
用一步前瞻把ORCA缺的那部分未来信息补回来。

**Q2：初赛用B样条优化，决赛为什么换成多项式基元？**

场景性质变了。初赛是静态的，可以离线做全局联合优化，B样条在平滑性上有优势。
决赛有动态障碍和跳变目标，必须每0.2秒重规划一次，
这时候需要的是能解析满足边界条件、可以大批量快速生成和筛选的候选族。
五次多项式正好满足。

**Q3：为什么不直接加大安全容差？**

容差是被动兜底，治不了根。病根是L1对"天上还有23架飞机"完全没有感知。
我们实测过，把到达容差从1.0降到0.9、再降到0.5，
两次都让基准场景反而退化了。这条路走不通。

## 二、参数与设计

**Q4：高度分层的层间距是怎么定的？**

不是调出来的，是推出来的。它必须大于L2局部垂直修正范围的两倍，
否则下层的避让动作会穿进相邻层。我们也试过更小的间距，
结果直接退化到提前失败，反过来印证了这个约束。

**Q5：横向走廊为什么要做降级阶梯，不能固定一个偏移量吗？**

因为横向平移和抬高的性质不一样。抬高是必然安全的——
障碍物是地面锚定的棱柱，横向无碰的路径抬高以后还是无碰。
但横向平移可能直接撞上远离中线的障碍物，所以必须逐档试、每档拿真几何校验、
不行就降级。

**Q6：目标滤波会不会造成跟踪滞后？**

会有，但这是必须付的代价。目标抖动的幅度是到达判定球半径的3倍——
如果直接跟着抖动走，飞机会一直在追一个晃动的点，**永远判不了"到达"**。
死区设在跟到达容差同一量级，小于判据的抖动直接忽略，大于判据的变化照常响应。

## 三、指标与结果

**Q7：评分基准是你们自己定的，会不会偏向自己？**

确实有这个风险，所以我们做了三件事。第一，基准是拿一次完整成功任务的
实测值定标的，让基准运行落在评分区间的中段，而不是贴着满分；
第二，代码注释里逐条标注了哪些是官方口径、哪些是工程假设；
第三，这个分数本来也不是比赛成绩，评委会用自己的尺子。

我们真正拿得出手的是原始实测值：24架全到、零违规、最小机间距3.011米。

**Q8：单次规划时长5.36秒是不是偏慢？**

要区分两个口径。5.36秒是一次性产出全部24条完整全局路径的离线过程；
在线滚动周期的耗时是177毫秒，比初赛的203毫秒还快。

优化空间我们也量过：把长距离的单段规划拆成两段拼接，耗时反而会下降，
因为A星的耗时对距离是超线性的。这是下一步的直接方向。

## 四、高速动态障碍物（⚠️ 重点，务必熟练）

**Q9：如果把动态障碍物的速度调得很快，你们的方案还能跑通吗？**

能。这组测试我们做过，而且一开始是失败的，后来定位清楚做了针对性改进。

我先说当时的情况。组委会给的参数里，最快的障碍物振幅300米、周期6秒，
峰值速度 **314米每秒**，是我们无人机最大速度的 **6倍多**。
第一次跑，在第165个周期就失败了。

诊断的结论是这样：有一个障碍物振幅500米、本体200乘40米，
它在横向的扫掠带宽达到 **1135米**，而我们的航路恰好从这条扫掠带正中间穿过。
当时9架机的候选轨迹被全部清空，而且这些候选的最小动态净空**中位数是0米**——
也就是说它们是直接从障碍物体内穿过去的。L2的局部横向权限只有正负20米，
对上千米的扫掠带完全无能为力。

更值得说的是另一个发现：我们回头去查那组"成功"的慢速参数，
发现**它的振幅和扫掠包络跟高速组是完全一样的**，它没出事，
纯粹是因为机队经过的时候障碍物恰好在扫掠带的另一端。换个初相位照样会失败。

所以这不是"障碍物变快才出的问题"，而是我们的方案对扫掠包络本来就没有防御，
两次结果都是相位决定的。

修复的思路是：**在L1建栅格的时候，把高速动态障碍物的完整周期扫掠包络
当作静态禁飞区标进去**，让全局A星从源头绕开。
配合着还做了三件事：给高速模式补了更大的减速候选，让飞机能在绕行拐角处减到
20到30米每秒；协调器在顺序预约失败时启动多机联合选择；
终端进场改成按编号错峰、并且从250米就开始对准真实目标，避免最后急转。

改完之后，这组高速参数的结果是：**24架全部抵达，584个周期，零碰撞，
最小机间距3.002米，最小静态净空1.875米，最小动态净空7.874米。**

---

**Q10（追问，一定要准备）：把动态障碍物当静态处理，是不是等于没做动态避障？
这样算不算取巧？**

这个问题问得好，我分三层回答。

**第一，我们做的是分级处理，不是一刀切。** L2局部规划层对全部三个动态障碍物
**始终使用真实的时变位置做逐时刻预测**，这一层没有任何简化。
扫掠包络只作用在L1全局引导层，而且只对峰值速度超过无人机2倍的障碍物启用。
那个78.5米每秒的障碍物全程走的都是动态预测路径，没有用包络。

**第二，包络是保守近似，我们是在让出空域，不是在放松约束。**
扫掠包络是障碍物可达位置的超集，避开包络必然意味着任何时刻都避开了障碍物本体。
代价是航程变长、能耗上升——实测总能耗从617涨到795，涨了将近三成。
这是实打实的成本，不是便宜。

**第三，也是最关键的一点：安全指标仍然是按真实障碍物计算的。**
最终报出来的最小动态净空7.874米，是无人机到真实障碍物本体的距离，
不是到包络的距离。包络只影响全局引导路径的生成，不参与任何指标核算。

补一句物理上的理由。在追逃问题里，**当障碍物速度超过你的最大速度时，
反应式规避在数学上就不成立了**——你既跑不过它，也来不及在预测时域内
横向躲开上千米的扫掠带。这个时候把可达集整体避开，
是唯一可证安全的策略。所以与其说这是取巧，不如说这是速度倒挂条件下的正解。

---

**Q11（可能追问）：那个2倍速度的阈值是怎么定的？**

坦率说，这是个工程取值，不是严格推导出来的。

理论上的分界点在1倍——也就是障碍物速度超过无人机最大速度时，
反应式规避就开始失效。我们取2倍是在这之上留了一段保守余量，
然后用实测验证了这个取值在两组参数下都工作正常。

这个阈值是暴露在配置文件里的，如果组委会有更明确的要求，改一个常量就行。
我们也考虑过干脆去掉阈值、对所有动态障碍物一律用包络，
这样行为更统一，代价是对慢速障碍物略保守一些。

## 五、鲁棒性与泛化

**Q12：泛化性测试里有失败的场景，怎么看待？**

暴露问题比掩盖问题有价值。那些失败精确定位了两个薄弱点：
一个是分配规则对数据顺序有隐含假设，另一个是终点切换区的拥堵机制。
如果我们只跑官方那一组数据，这两个问题会一直藏着，
等到评委复跑的时候才爆出来。

**Q13：这套方法能推广到不同的机队规模吗？**

分层和分组的逻辑都是从数据算出来的，代码里没有写死机队数量。
但我要诚实说明，完整验证主要还是在24机、19个障碍物这组数据上做的，
规模大幅变化之后的表现还需要进一步测试，这一点我们不回避。

---

## ⚠️ 上台前必须确认的两件事

1. **PPT第20、21页的数字是慢速参数那一版的**（116.60秒、626.920、1.1500、5.360s）。
   如果你们现在把正式CSV配置换成了高速参数，**现场复跑会得到另一组数**
   （584周期、795.267、0.8057）。要么改PPT，要么现场演示时明确说明用的是哪组参数。

2. **最小机间距3.002米只剩2毫米余量**（慢速组是3.008、原基准3.011）。
   如果评委要求现场换种子复跑，这个数有翻车风险。建议提前多跑几个种子确认。
