# Counter_Drone final_version 代码架构与算法说明

对照复赛科目三题面，梳理这一版的整体架构、设计思路、规划算法与控制策略。
核实方式：实跑 + 读码，文中数据均为本机实测（本机比 7945HX 慢，耗时偏保守）。

---

## 〇、先说这一版跑通了

```
模式 competition（3500m 切换 + 1Hz 正弦 + 随机误差，全开）
success=True   cycles=614   mission=122.8 s   arrivals=24/24
min UAV 间距 3.001 m（限 3.0）   min 离障 2.871 m（限 1.5）
单周期 mean 162.8 ms / p95 273.7 ms / max 507.9 ms
```

之前那版在终点切换后会在 cycle 537 崩掉、只有 2/24 到达。这一版解决了，关键是
两处终端参数的改动（见第五节）。

---

## 一、总体架构

三层结构，自上而下：

```
┌─ L1 全局引导层（离线一次）────────────────────────────┐
│  稀疏栅格加权 A*  →  24 条参考航路                     │
│  转弯预瞄跟踪器   →  每周期给出前瞄目标点              │
└───────────────────────┬───────────────────────────────┘
                        │ 引导方向 / 引导目标
┌─ L2 滚动时域层（每 0.2 s）────────────────────────────┐
│  五次多项式基元库(60条) → 静态筛选 → 动态筛选 → 排序   │
│  终端策略在距终点 250 m 处接管                         │
└───────────────────────┬───────────────────────────────┘
                        │ 24 机 × 60 候选
┌─ L3 集群协调层（每 0.2 s）────────────────────────────┐
│  动态优先级 → 时间索引预约 → 一步前瞻可行性门          │
│  失败时触发有界局部联合协调                            │
└───────────────────────┬───────────────────────────────┘
                        │ 每机选定 1 条
                   执行前 0.2 s 前缀，状态推进
```

设计思路一句话概括：**用离线的全局 A\* 解决"走哪条拓扑通道"，用在线的滚动时域
解决"动态障碍和动态终点"，用集群协调解决"24 机互不相撞"。** 三件事分层解耦，
任何一层的失败都能被上一层诊断定位。

### 目录结构

```
code/run_example/
├── mamp/
│   ├── configs/subject3_config.py        全部参数，题面给定值与工程假设分区标注
│   ├── agents/obstacle.py                碰撞体：圆柱/长方体/斜放长方体 + 运动模型
│   ├── envs/subject3_environment.py      19 障碍 → 23 碰撞体，起点与两张终点表
│   ├── collision/
│   │   ├── static_primitive_collision.py 基元 vs 静态障碍
│   │   ├── dynamic_primitive_collision.py 基元 vs 运动障碍（解析预测）
│   │   └── swarm_trajectory_collision.py  机间轨迹对距离审计
│   ├── planners/
│   │   ├── global_guide.py               L1：稀疏栅格 A* + 转弯预瞄跟踪（664 行，最大模块）
│   │   ├── quintic_primitive.py          L2：五次多项式基元库，向量化生成
│   │   ├── primitive_selector.py         L2：静态筛选与代价排序
│   │   ├── dynamic_primitive_selector.py L2：动障碍筛选
│   │   ├── receding_horizon_planner.py   L2：单机滚动时域封装
│   │   ├── terminal_policy.py            L2：末端锁定基元与目标导向重排序
│   │   ├── goal_manager.py               终点管理：3500m 切换 + 动态噪声 + 滤波
│   │   ├── swarm_base_batch_planner.py   L3：24 机候选池化批量求解
│   │   ├── swarm_coordinator.py          L3：动态优先级 + 时间索引预约
│   │   ├── local_joint_coordinator.py    L3：有界局部联合协调（预约失败时兜底）
│   │   └── recursive_feasibility_coordinator.py L3：一步前瞻可行性门
│   ├── simulation/
│   │   ├── final_validation_evaluator.py 整场任务编排与结果汇总
│   │   ├── swarm_rolling.py              同步滚动编排
│   │   ├── swarm_shadow_rollout.py       一步前瞻影子推演
│   │   └── swarm_failure_diagnostic.py   失败快照
│   └── ...
├── run_phase6b_final_validation.py       场景构建入口
├── animate_phase6b_final_validation.py   动画入口（README 指向这个）
└── animate_subject3_environment_3d.py    场地三维绘制
```

---

## 二、L1 全局引导层

### 算法：稀疏栅格加权 A\*

| 参数 | 值 | 说明 |
|---|---|---|
| 栅格 | 20 × 5 × 10 m（x/y/z） | 沿主航向粗、垂向细 |
| 启发权重 | `W = 1.2` | 加权 A\*，牺牲最优性换速度 |
| 边采样间距 | 2.5 m | 连边时沿线段采样查碰撞 |
| 端点搜索半径 | 6 格 | 起终点不在格点上时就近吸附 |
| 垂向预留 | 10 m | 中心线预留，物理限高仍由基元校验把关 |

**为什么用稀疏栅格而不是 ESDF 或 RRT\***：场地 5000 × 1150 × 100 m，细栅格建图
成本太高；而障碍全是规则几何体，解析距离查询是 O(1)，稀疏栅格 + 线段采样就够用。

### 跟踪器：转弯预瞄（Turn-aware preview）

`GlobalGuideTracker` 不是简单的最近点跟踪，而是根据**前方航路的转角大小**动态
调整预瞄距离：

- 基础预瞄 60 m
- 转角大 → 预瞄拉近（最小 20 m），提前开始转
- 直线段 → 预瞄拉远（最大 300 m），减少无谓摆动
- 转弯加速度参考比 `0.3`，即预留 30% 的过载预算给转弯

这一层解决的是"参考轨迹给得太晚会来不及转、给得太早会切角"的问题。

### 实测

```
建图（静态环境，一次）         0.311 s
24 条全局路径                   9.588 s      ← 见第六节，这是最大的问题
```

---

## 三、L2 滚动时域层

### 基元库：五次多项式

每个规划周期，从当前状态 `(p, v, a)` 出发，向引导方向生成候选轨迹：

```
横向偏置 (-20, -10, 0, +10, +20) m      5 档
垂向偏置 (-10, 0, +10) m                3 档
末速增量 (-10, 0, +10, +20) m/s         4 档
                                    = 60 条候选
时域 3.0 s，生成采样 0.05 s
```

五次多项式由**闭式解**给出（起点的位置/速度/加速度 + 终点的位置/速度/加速度共
六个边界条件恰好定六个系数），全程向量化，没有迭代求解。多项式基矩阵有缓存。

**为什么是五次而不是三次**：五次能同时约束两端的加速度，保证周期拼接处加速度
连续，直接对应平滑性指标里的曲率积分。

### 两级筛选

**第一级 — 静态筛选（`primitive_selector.py`）**

以 `VALIDATION_DT = 0.01 s` 重新细采样（比生成采样密 5 倍），逐点校验：

- 速度 ≤ 50 m/s
- 高度 ∈ [0, 100] m
- xz 合成过载 ≤ 2g，向上 ≤ 2g，向下 ≤ 1g
- 离障距离 ≥ 1.5 m（另有采样间运动 guard）

通过的候选按四项加权代价排序：

```
代价 = 1.00 × 进程（沿引导方向推进多少，越大越好）
     + 0.25 × 偏置（偏离引导中心线的惩罚）
     + 0.10 × 曲率（平滑性）
     + 0.20 × 离障（期望 5 m，低于则惩罚）
```

**第二级 — 动态筛选（`dynamic_primitive_selector.py`）**

对 10/11/12 号运动障碍做**解析位姿预测**（不是把动障碍当静态膨胀），在基元的
每个采样时刻算出障碍当时的位姿再查距离，另加一个由障碍平动速度和箱体旋转角速度
推出的保守 guard 覆盖采样间隙。

### 终端策略（`terminal_policy.py`）

距终点 250 m 触发，切换到"锁定终端基元"模式：

- 基元终点直接指向目标点而非引导前瞄点
- 重排序代价改为：目标距离权重 2.0 + 参考速度权重 0.5
- **已执行前缀的目标权重 5.0** —— 这一条很关键：固定时域的滚动控制器会"永远
  把到达推迟到下一周期"，只奖励终点吸引是不够的，必须奖励这 0.2 s 实际飞出去的
  那一段

---

## 四、L3 集群协调层

这一层是 24 机不相撞的保证，三级递进。

### 第一级：动态优先级 + 时间索引预约

优先级排序规则（`dynamic_priority`，按字典序）：

```
1. 可行候选数少的优先      —— 选择余地小的先占位
2. 冲突度高的优先          —— 和别人纠缠多的先定
3. 离障间隙小的优先        —— 处境危险的先定
4. 机号                    —— 保证确定性可复现
```

按此顺序逐机预约：先定的机把自己的轨迹写进**时间索引预约表**，后定的机在自己的
60 条候选里挑第一条与所有已预约轨迹保持 ≥3 m 的（`first_safe` 模式，找到即停，
不穷举）。候选分块大小 4，批量做轨迹对距离审计。

### 第二级：一步前瞻可行性门（`recursive_feasibility_coordinator.py`）

顺序预约有个经典毛病：**这一周期选的动作看似安全，却把自己逼进下一周期无解的
死角。** 所以在接受某个候选前，先做一次影子推演（`swarm_shadow_rollout.py`），
检查执行它之后下一周期是否还生得出可行解。过不了就换候选。

### 第三级：有界局部联合协调（`local_joint_coordinator.py`）

当顺序预约整体失败时，不直接判负，而是：

1. 找出冲突聚簇（互相纠缠的那几架）
2. 在**聚簇内部**做有界联合枚举（`itertools` 组合，规模受限）
3. 联合解出一组互相兼容的候选

这是"顺序贪心 + 局部最优"的混合策略——全局用贪心保证速度，局部用枚举保证质量。

---

## 五、终点管理：切换与噪声

### 3500 m 切换

```python
should_switch_goal(position) :=  ‖position − (0,0,0)‖ ≥ 3500
```

**每架独立判断，参考原点**（这与主办方确认的口径一致）。切换是单向的
（`goal_switched` 标志一旦置位不回退），切换后触发该机的全局引导重规划。

### 动态终点

```
观测终点 = 基准终点 + 1 Hz 正弦分量 + 分段随机误差
再经一阶滤波（α = 0.5）+ 死区（1 m）
```

滤波和死区是工程加的：终点每秒抖一下，如果规划器照单全收会跟着抖，把平滑性和
能耗都做坏。死区 1 m 意味着抖动幅度小于 1 m 时直接忽略。

### 这一版解决 5 m 密集终点的两处关键改动

表3 侧向组和后向组的相邻终点只差 **5 m**，而机间安全距离是 3 m。之前那版在这里
崩掉（协调器报 UAV 间最小只能做到 1.52 m）。这一版改了两个参数：

```python
# subject3_config.py
GOAL_TOLERANCE_ASSUMED = 1.0     # 原 3.0
# 注释原文：3 m 到达球允许无人机贴到最靠近邻机的一侧，可能留不足 3 m 间距。
#          1 m 让捕获判定不含糊，同时也是更严格的终端精度标准。

# run_phase6b_final_validation.py
terminal_capture_speed = 0.      # 原 10.0
# 注释原文：密集终点只有 5 m 间距。零捕获速度让每架稳稳落进自己的 3 m 到达球，
#          而不会从已经悬停就位的邻机身上穿过去。
```

思路是对的：**把到达球从 3 m 缩到 1 m，等于给每架划了一个不会重叠的终端领域**
（5 m 间距 − 2×1 m 半径 = 3 m 净空，正好等于安全距离）；捕获速度归零则避免高速
掠过邻机。实测 min UAV 间距 3.001 m，刚好卡在限值上——**没有裕度**，这是隐患，
见第六节。

---

## 六、控制器

严格说这套系统**没有独立的控制器**，它是一个纯轨迹规划器。执行方式是：把选中
基元的前 0.2 s 前缀按 0.01 s 采样直接推进状态。控制层的职责由三处替代：

| 控制职责 | 由谁承担 |
|---|---|
| 轨迹跟踪 | 转弯预瞄跟踪器（L1）把引导航路转成每周期的前瞄目标 |
| 动力学限幅 | 静态筛选（L2）逐点校验速度/高度/过载，不满足的候选直接剔除 |
| 末端捕获 | 终端策略（L2）减速停靠 + 零捕获速度 |
| 避碰 | 集群协调（L3）时间索引预约 |

初赛那版有独立的 SCA 局部避碰层（速度障碍法 + KD 树邻域检索），**这一版被删掉
了**。取舍是合理的：复赛机间安全距离从 10 m 放宽到 3 m，而集群协调层已经在规划
阶段保证了间距，再叠一层反应式避碰反而会和规划打架。

---

## 七、对照五项考核指标的现状

| # | 指标 | 权重 | 现状 | 评价 |
|---|---|---|---|---|
| 1 | 单次规划时长 | 30% | **24 条全局路径 9.588 s** | ⚠️ 严重问题 |
| 2 | 无人机抵达时长 | 30% | 122.8 s（下界约 103 s） | 有约 20 s 差距 |
| 3 | 集群总能量消耗 | 20% | **无法计算** | ⚠️ 评估器被删了 |
| 4 | 路径安全性 | 10% | 机间 3.001 / 离障 2.871 | 通过但无裕度 |
| 5 | 路径平滑性 | 10% | **无法计算** | ⚠️ 评估器被删了 |

> **更新（L1 空域分区）**：定位到"零裕度"的结构性根因并已解决。A\* 的代价是纯欧氏
> 距离、y 方向无加权，而 24 架的起终点几乎相同，导致规划出 24 条近乎重合的航路——
> 实测巡航段各机航迹的横向间距只有 0.09~1.01 米，整个编队共面挤成一束，全部避撞
> 压力压给 L2/L3 每 0.2 秒现场化解。现按机号交错分配 3 个巡航高度层（30/60/90 米，
> 间隔须 >20 米否则被 L2 的 ±10 米垂直偏移重新搅混），并叠加按终点 z 等比放大的
> 横向走廊（逐级降档 2.5/2.0/1.5，逐段用真实障碍物几何校验）。实测（competition
> 模式、种子 0）：硬编码基准 614 周期/122.80 秒 → **583 周期/116.60 秒**，
> CSV 配置场景 **从第 112 周期失败（0/24 到达）变为 583 周期成功（24/24 到达）**，
> 且两种输入收敛到同一结果——此前几毫米的 CSV 舍入误差就能决定成败，现在不再敏感。
> 平滑性 1.874→1.104，能耗 617.7→625.9（+1.3%），加权总分 75.6→77.8。
>
> **更新（模块化封装补丁）**：能耗与平滑性已补齐计算——`FinalValidationEvaluator.run()`
> 现在保留每个 0.2s 周期内的细粒度轨迹采样，`mamp/simulation/mission_metrics.py`
> 对其积分得到 Σ∫|需用过载|dt 与 ∫κ²ds，随 `run_with_config.py` 一并输出到
> `trajectory_output.json`。规划时长按当前架构如实测量上报（约9.6秒量级），
> 未做本节末尾提到的架构调整或优化。同时修复了 `run_with_config.py` 调用
> `formal_case()` 时的签名不匹配 bug（CSV 传入的起点/终点此前根本没有到达规划器）。

### ⚠️ 问题 1：指标 1 的口径

主办方已确认"单次规划时长 = 一次产出所有无人机路径的用时"。这一版产出 24 条
完整路径需要 **9.588 s**。作为对比，初赛 HFSP 那套一次产出全部 24 条完整轨迹
（含 B 样条优化和 TOPP）只要 **203 ms**——差 **47 倍**。

滚动时域架构的单周期 162.8 ms 不能拿来报这一项，因为它产出的是 3 秒轨迹片段，
不是"所有无人机路径"。

**这是 30% 的分，且不是调参能解决的。**

### ⚠️ 问题 2：评估器被删除

这一版相比之前删掉了 `mamp/evaluator/metrics.py`、整个 `tests/`、
`trajectory_optimizer.py` 和 SCA 策略层。后果是**五项指标里的能耗和平滑性现在
算不出来**，提交时没有数据可填。

### ⚠️ 问题 3：安全裕度为零

min UAV 间距 3.001 m，限值 3.0 m，裕度 **1 毫米**。主办方用自己的数据验证时，
障碍位置和运动参数都会变，这个裕度经不起任何扰动。

### ⚠️ 问题 4：NumPy 2.x 不兼容

`primitive_selector.py`、`swarm_base_batch_planner.py`、`single_uav_rolling.py`
三处仍用 `np.trapz`，该函数在 NumPy 2.0 已删除。主办方环境若是新 numpy，**代码
直接报错**。我这次是打了运行时垫片才跑起来的。

### 其他

- 障碍运动幅值与题面图 2 不符（10 号应为 ±300 m 沿 x，代码里是 ±150 m）
- `envs/subject3_environment.py` 的 `should_switch_goals()` 仍抛 NotImplementedError
  （不影响运行，`GoalManager` 里有自己的实现，但属于死代码）
- 障碍表硬编码，主办方"增加障碍物数量"后无法直接加载

---

## 八、结论与建议

**这一版把功能跑通了，这是实质进展**——终点切换、动态终点、24/24 到达，之前
都做不到。集群协调那三级递进（优先级预约 → 前瞻可行性门 → 局部联合枚举）是
这套代码里最有分量的设计，答辩时值得重点讲。

**但指标 1 的架构问题没解决，30% 的分悬着。** 滚动时域天生不产出"完整路径"，
这不是代码质量问题，是架构与评分口径的错配。

距 9/18 24:00 还有 3 天，我的建议：

1. **立刻做**：补回评估器（能耗按 ∫\|需用过载\|dt、平滑性按 ∫κ²ds），否则提交表
   里有两项空白。这个工作量小，半天。
2. **立刻做**：NumPy 2 兼容垫片 + 障碍幅值修正。半小时的事，但不做可能直接翻车。
3. **重点决策**：指标 1 怎么办。两条路——
   - 把初赛 HFSP 的 B 样条批量优化 + TOPP 后端接过来做"一次产出全路径"，
     滚动层退居为执行期的动态响应。工作量大但收益 30%。
   - 或者把全局引导的 9.588 s 优化到 1 秒级（共享搜索图、批量 A\*、
     多起点单次搜索），报 1 秒。收益小但风险低。
4. **提交前必做**：把安全裕度从 1 毫米拉开。到达球 1 m 可以再收紧到 0.8 m，或者
   在终端策略里加一条"邻机已就位则不得进入其 1.5 m 邻域"的硬约束。
