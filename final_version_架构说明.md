# Counter_Drone final_version 代码架构与算法说明

对照复赛科目三题面，梳理这一版的整体架构、设计思路、规划算法、控制策略，以及
面向评委的模块化接口。核实方式：实跑 + 读码，文中数据均为实测（不同机器耗时略有
差异，已标注）。

本版相对上一版的两个核心变化：
1. **L1 空域分区**（本节重点）——定位到"零裕度"的结构性根因（24 架规划出近乎
   重合的航路）并给出解法，副作用是任务时间和路径平滑性也一并改善。
2. **模块化封装接口**——CSV 驱动的配置输入 + 独立评分脚本，5 项官方指标现在
   全部可计算。

---

## 〇、先说这一版跑通了

```
模式 competition（3500m 切换 + 1Hz 正弦 + 随机误差，全开），种子 0
success=True   cycles=583   mission=116.60 s   arrivals=24/24
min UAV 间距 3.010~3.011 m（限 3.0）   min 离障 2.355 m（限 1.5）
集群总能量消耗 625.9   路径平滑性(均值) 1.104
单次规划时长 ~5.4~8.2 s（硬件相关，见第二节）
```

**关键进展：硬编码默认场景和 CSV 配置场景现在收敛到同一个结果**（周期数、任务
时间、机间距、能耗、平滑性全部一致，仅规划时长因硬件而异）。这在上一版是做不到
的——`config/start_points.csv` 里的坐标被四舍五入到 2 位小数，和代码内部
`np.linspace` 算出的全精度浮点数差了最多约 4.8 毫米，这几毫米的输入噪声曾经足以
让 CSV 场景在第 112 周期（任务时间 22.4 秒，飞行早期）就报"no coordinated
candidate"失败、0/24 到达，而硬编码场景在同样种子下能跑满全程。用代码自带的
失败诊断工具（`swarm_failure_diagnostic.py`）查过：那次失败时 14 号机 8 条候选
轨迹里离 17 号机最近的只有 0.41 米，换任何优先级顺序都无解——分类结果是"候选
轨迹族本身不够用"，不是差一点点的边缘情况。根因排查详见第二节。

版本演进备忘（供理解裕度问题的来龙去脉）：
```
初版：终点切换后 cycle 537 崩溃，仅 2/24 到达
  → 改两个终端参数（GOAL_TOLERANCE_ASSUMED 3.0→1.0，terminal_capture_speed 10→0）
    跑通，但机间最小距离精确卡在 3.001 m，裕度只有 1 毫米
  → 本版：L1 空域分区，同时解决零裕度和 CSV 精度敏感性
```

---

## 一、总体架构

三层结构，自上而下：

```
┌─ L1 全局引导层（离线一次）────────────────────────────┐
│  稀疏栅格加权 A*  →  24 条参考航路                     │
│  空域分区：高度分层 + 横向走廊  →  航路互相错开        │
│  转弯预瞄跟踪器   →  每周期给出前瞄目标点              │
└───────────────────────┬───────────────────────────────┘
                        │ 引导方向 / 引导目标
┌─ L2 滚动时域层（每 0.2 s）────────────────────────────┐
│  五次多项式基元库(60条) → 静态筛选 → 动态筛选 → 排序   │
│  终端策略在距终点 250 m 处接管                         │
└───────────────────────┬───────────────────────────────┘
                        │ 24 机 × 60 候选
┌─ L3 集群协调层（每 0.2 s）────────────────────────────┐
│  动态优先级 → 时间索引预约 → 一步前瞻可行性门          │
│  失败时触发有界局部联合协调                            │
└───────────────────────┬───────────────────────────────┘
                        │ 每机选定 1 条
                   执行前 0.2 s 前缀，状态推进
```

设计思路一句话概括：**用离线的全局 A\* 加空域分区解决"走哪条拓扑通道、彼此别
挤在一起"，用在线的滚动时域解决"动态障碍和动态终点"，用集群协调解决"24 机
互不相撞"。** 三件事分层解耦，任何一层的失败都能被上一层诊断定位。

### 目录结构

```
code/
├── config/                                障碍物/起降点表格（评委输入接口）
│   ├── obstacles.csv
│   ├── start_points.csv
│   ├── goal_points_initial.csv
│   └── goal_points_changed.csv
└── run_example/
    ├── config_loader.py                   CSV → Python 数据结构
    ├── run_with_config.py                 标准化入口：读CSV → 跑规划 → 出JSON
    ├── evaluate_metrics.py                独立评分脚本：读JSON → 按官方权重算分
    ├── animate_phase6b_final_validation.py 动画入口，支持 --config-dir
    ├── run_phase6b_final_validation.py    场景构建（formal_case，空域分区落地处）
    └── mamp/
        ├── configs/subject3_config.py        全部参数，题面给定值与工程假设分区标注
        ├── agents/obstacle.py                碰撞体：圆柱/长方体/斜放长方体 + 运动模型
        ├── envs/subject3_environment.py      19 障碍 → 23 碰撞体，起点与两张终点表
        ├── collision/
        │   ├── static_primitive_collision.py 基元 vs 静态障碍
        │   ├── dynamic_primitive_collision.py 基元 vs 运动障碍（解析预测）
        │   └── swarm_trajectory_collision.py  机间轨迹对距离审计
        ├── planners/
        │   ├── global_guide.py               L1：稀疏栅格 A* + 转弯预瞄跟踪
        │   ├── quintic_primitive.py          L2：五次多项式基元库，向量化生成
        │   ├── primitive_selector.py         L2：静态筛选与代价排序
        │   ├── dynamic_primitive_selector.py L2：动障碍筛选
        │   ├── receding_horizon_planner.py   L2：单机滚动时域封装
        │   ├── terminal_policy.py            L2：末端锁定基元与目标导向重排序
        │   ├── goal_manager.py               终点管理：3500m 切换 + 动态噪声 + 滤波
        │   ├── swarm_base_batch_planner.py   L3：24 机候选池化批量求解
        │   ├── swarm_coordinator.py          L3：动态优先级 + 时间索引预约
        │   ├── local_joint_coordinator.py    L3：有界局部联合协调（预约失败时兜底）
        │   └── recursive_feasibility_coordinator.py L3：一步前瞻可行性门
        └── simulation/
            ├── final_validation_evaluator.py 整场任务编排与结果汇总
            ├── mission_metrics.py            能耗(Σ∫|需用过载|dt)与平滑性(∫κ²ds)积分
            ├── swarm_long_horizon_evaluator.py 官方安全指标审计（真值来源，不参与规划决策）
            ├── swarm_rolling.py              同步滚动编排
            ├── swarm_shadow_rollout.py       一步前瞻影子推演
            └── swarm_failure_diagnostic.py   失败快照 → 分类诊断（本节案例用的就是它）
```

---

## 二、L1 全局引导层

### 算法：稀疏栅格加权 A\*

| 参数 | 值 | 说明 |
|---|---|---|
| 栅格 | 20 × 5 × 10 m（x/y/z） | 沿主航向粗、垂向细 |
| 启发权重 | `W = 1.2` | 加权 A\*，牺牲最优性换速度 |
| 边采样间距 | 2.5 m | 连边时沿线段采样查碰撞 |
| 端点搜索半径 | 6 格 | 起终点不在格点上时就近吸附 |
| 垂向预留 | 10 m | 网格顶部预留，实际网格覆盖 y∈[0,90]，物理限高 100m 仍由基元校验把关 |

**为什么用稀疏栅格而不是 ESDF 或 RRT\***：场地 5000 × 1150 × 100 m，细栅格建图
成本太高；而障碍全是规则几何体，解析距离查询是 O(1)，稀疏栅格 + 线段采样就够用。

### 根因诊断：为什么零裕度不是阈值问题，而是航路重合问题

A\* 的代价函数是**纯欧氏距离**，y 方向没有任何加权；而 24 架的起点（全部
x=0, y=0, z∈[-40,+40]）和终点（全部 x=5000, y=50, z∈[-100,+130]）几乎相同。
独立规划的结果是 **24 条几乎重合的航路**：实测巡航段各机航迹的横向间距只有
**0.09~1.01 米**——整个编队被安排成一个共面的紧密束，横向起点间距只有 3.478
米（只有 3 米安全距离的 116%），全部避撞压力被甩给 L2/L3 每 0.2 秒现场化解。

用失败诊断工具复现第 112 周期那次 CSV 场景失败，结论印证了这一点：14 号机当时
8 条可行候选里最近的一条离 17 号机只有 0.41 米，诊断分类是"B. 候选轨迹族本身
不够用"，换任何优先级顺序（诊断工具的 oracle 重排）都无解——不是协调器排序
不够聪明，是那一刻 24 架挤在同一条窄带里，物理上就没有解。

### 空域分区：高度分层 + 横向走廊

**做法**：24 架按 `机号 % 3` **交错**分组（不能按块分——起点 z 相邻的两架只隔
3.478 米，交错后同层内相邻两架变成隔 10.4 米），每组分配一个巡航高度
**y = 30 / 60 / 90 米**。层间隔必须 **> 20 米**：L2 基元库自己有
`PRIMITIVE_VERTICAL_OFFSETS = (-10, 0, 10)` 的垂直偏移，间隔不够会被 L2 重新
搅混。在此基础上叠加横向走廊：巡航段的 z 坐标按**终点 z 等比放大**
（`corridor_z = goal_z × scale`），保证走廊顺序和终点顺序一致，收拢时不交叉；
每架从 `scale = 2.5 → 2.0 → 1.5` 逐级尝试，取第一个每段都通过
`segment_is_static_safe()`（真实障碍物几何校验）的，全部失败则退回只分层，
再失败退回原始航路。

**为什么用真实几何校验而不是遮蔽 A\* 占用栅格**：规划器自己的 LOS 剪枝
（`_prune`）就是按真实障碍物几何做直线可达性判断的，如果只在栅格层面挡路，
剪枝会直接把爬升/横移拉直拉平，白改。所以做法是：复用 A\* 已经算好的水平路线
（它本就避开了所有静态障碍），只对新增的爬升/横移/下降段逐段做真实几何校验。

**只作用于巡航段**：`GUIDE_BAND_CLIMB_END_X=600` 起爬升，`CRUISE_END_X=4000`
前保持分区，`DESCENT_END_X=4900` 前完全归位到原航路，此后与未分区版本完全一致。
必须在这个范围内完成收放，因为：
- 3500 米触发终点切换，切换后三组终点分别在 y=50/20/10、z 分散到 ±575，
  终端策略接管，这一段的几何完全由题面给定，分区帮不上忙；
- 17 号障碍是一堵墙（x=4600，z∈[-135,+135]，高 95 米），而 A\* 网格高度上限
  只有 90 米——**翻不过去**，终点恰好在它正后方，所有航路最后都得贴地绕过去。

**高层顺带减少绕障**（实测各高度需要绕开的静态障碍数，共 16 个可参与 L1 规划
的静态障碍——10/11/12 号是动态障碍，L1 网格不处理）：

| 巡航高度 | 需绕开的静态障碍数 |
|---|---|
| 50 m | 14 |
| 60 m | 11 |
| 70 m | 9 |
| 75 m | 8 |
| 80~85 m | 6 |
| 90 m（网格上限） | 4（仅 7/9/14/17 号） |

**走廊的实际生效情况**：24 架里只有 90 米层的 3 架真正保住了走廊，其余全部
降级到"无走廊"——因为实测发现低空所有 A\* 航路都被障碍物挤进同一条横向窄缝
（z 落在 -8 到 -13 米之间），不管起点终点差多少，低空根本没有横向散开的空间；
只有 90 米层（只剩 4 个障碍够得着）才有余地。就是这几架被挪开的航路，把"只分层
不加走廊"的终段失败变成了完整任务——说明高度仍是这个场地里能用来分离的主轴，
走廊是巡航段的补充手段，不是独立解法。

**实测结果**（competition 模式，种子 0）：

| 变体 | 硬编码基准 | CSV 配置场景 |
|---|---|---|
| 无分区（更早版本） | 614 周期 成功 24/24（122.80s） | 112 周期 **失败** 0/24 |
| 仅高度分层 30/60/90 | 531 周期 **失败** | 562 周期 **失败** 1/24 |
| 仅高度分层 30/50/70 | 382 周期 **失败** | — |
| 分层限定在切换前归位 | 365 周期 **失败** | — |
| **分层 + 横向走廊（本版）** | **583 周期 成功 24/24（116.60s）** | **583 周期 成功 24/24（116.60s）** |

单独加分层会**把失败从巡航段搬到终段**（两个场景都在 106~112 秒左右死掉，
而完整任务只要 116.6 秒），说明"下降归位"这个动作本身就是 L2 单机层容易卡住
的地方；分层限定在切换前更早归位反而更差（365 周期），提前做只是让失败来得
更早。必须分层与走廊同时生效才能把两个场景都送到终点，且两种输入收敛到同一
结果——CSV 舍入误差不再改变结局。

副作用是正向的：任务时间从 122.80 秒降到 **116.60 秒**（高层绕障更少），
路径平滑性从 1.874 降到 **1.104**（改善 41%），能耗从 617.7 升到 625.9
（+1.3%，代价很小）。

### 跟踪器：转弯预瞄（Turn-aware preview）

`GlobalGuideTracker` 不是简单的最近点跟踪，而是根据**前方航路的转角大小**动态
调整预瞄距离：

- 基础预瞄 60 m
- 转角大 → 预瞄拉近（最小 20 m），提前开始转
- 直线段 → 预瞄拉远（最大 300 m），减少无谓摆动
- 转弯加速度参考比 `0.3`，即预留 30% 的过载预算给转弯

这一层解决的是"参考轨迹给得太晚会来不及转、给得太早会切角"的问题。空域分区
产生的爬升/横移/下降折角会经过同一套转弯预瞄处理，不需要额外适配。

### 实测

```
建图（静态环境，一次）         约 0.37 s
24 条全局路径（含空域分区）    约 5.4~8.2 s（硬件相关，见第七节问题1）
```

规划耗时相比分区前基本不变——分区只是对已经算好的水平路径做插值和几何校验，
没有增加 A\* 调用次数（仍是每架一次 `plan()`）。

---

## 三、L2 滚动时域层

### 基元库：五次多项式

每个规划周期，从当前状态 `(p, v, a)` 出发，向引导方向生成候选轨迹：

```
横向偏置 (-20, -10, 0, +10, +20) m      5 档
垂向偏置 (-10, 0, +10) m                3 档
末速增量 (-10, 0, +10, +20) m/s         4 档
                                    = 60 条候选
时域 3.0 s，生成采样 0.05 s
```

五次多项式由**闭式解**给出（起点的位置/速度/加速度 + 终点的位置/速度/加速度共
六个边界条件恰好定六个系数），全程向量化，没有迭代求解。多项式基矩阵有缓存。

**为什么是五次而不是三次**：五次能同时约束两端的加速度，保证周期拼接处加速度
连续，直接对应平滑性指标里的曲率积分。

### 两级筛选

**第一级 — 静态筛选（`primitive_selector.py`）**

以 `VALIDATION_DT = 0.01 s` 重新细采样（比生成采样密 5 倍），逐点校验：

- 速度 ≤ 50 m/s
- 高度 ∈ [0, 100] m
- xz 合成过载 ≤ 2g，向上 ≤ 2g，向下 ≤ 1g
- 离障距离 ≥ 1.5 m（另有采样间运动 guard）

通过的候选按四项加权代价排序：

```
代价 = 1.00 × 进程（沿引导方向推进多少，越大越好）
     + 0.25 × 偏置（偏离引导中心线的惩罚）
     + 0.10 × 曲率（平滑性）
     + 0.20 × 离障（期望 5 m，低于则惩罚）
```

**第二级 — 动态筛选（`dynamic_primitive_selector.py`）**

对 10/11/12 号运动障碍做**解析位姿预测**（不是把动障碍当静态膨胀），在基元的
每个采样时刻算出障碍当时的位姿再查距离，另加一个由障碍平动速度和箱体旋转角速度
推出的保守 guard 覆盖采样间隙。

### 终端策略（`terminal_policy.py`）

距终点 250 m 触发，切换到"锁定终端基元"模式：

- 基元终点直接指向目标点而非引导前瞄点
- 重排序代价改为：目标距离权重 2.0 + 参考速度权重 0.5
- **已执行前缀的目标权重 5.0** —— 这一条很关键：固定时域的滚动控制器会"永远
  把到达推迟到下一周期"，只奖励终点吸引是不够的，必须奖励这 0.2 s 实际飞出去的
  那一段

---

## 四、L3 集群协调层

这一层是 24 机不相撞的保证，三级递进。

### 第一级：动态优先级 + 时间索引预约

优先级排序规则（`dynamic_priority`，按字典序）：

```
1. 可行候选数少的优先      —— 选择余地小的先占位
2. 冲突度高的优先          —— 和别人纠缠多的先定
3. 离障间隙小的优先        —— 处境危险的先定
4. 机号                    —— 保证确定性可复现
```

按此顺序逐机预约：先定的机把自己的轨迹写进**时间索引预约表**，后定的机在自己的
60 条候选里挑第一条与所有已预约轨迹保持 ≥3 m 的（`first_safe` 模式，找到即停，
不穷举）。候选分块大小 4，批量做轨迹对距离审计。

### 第二级：一步前瞻可行性门（`recursive_feasibility_coordinator.py`）

顺序预约有个经典毛病：**这一周期选的动作看似安全，却把自己逼进下一周期无解的
死角。** 所以在接受某个候选前，先做一次影子推演（`swarm_shadow_rollout.py`），
检查执行它之后下一周期是否还生得出可行解。过不了就换候选。

### 第三级：有界局部联合协调（`local_joint_coordinator.py`）

当顺序预约整体失败时，不直接判负，而是：

1. 找出冲突聚簇（互相纠缠的那几架）
2. 在**聚簇内部**做有界联合枚举（`itertools` 组合，规模受限）
3. 联合解出一组互相兼容的候选

这是"顺序贪心 + 局部最优"的混合策略——全局用贪心保证速度，局部用枚举保证质量。

### 失败诊断工具（`swarm_failure_diagnostic.py`）

当协调器报"no coordinated candidate"时，这个只读工具能把失败复盘成四类：

```
A. priority starvation           —— 换个优先级顺序就能解，纯排序问题
B. candidate family insufficient —— 候选轨迹族本身不够用，换顺序也没用
C. arrived UAV reservation blocking —— 被已到达/悬停的邻机挡住
D. true geometric deadlock       —— 几何上真的无解
```

方法是拿失败瞬间的候选集重放：给失败机换到最优先（oracle 重排）看是否变好，
和已到达机的预约做隔离测试看是否是它们挡路。第二节的 0.41 米案例就是用它测出
"B 类"的，这个分类结果直接指向了 L1 航路重合的根因，而不是继续在 L3 调阈值。

---

## 五、终点管理：切换与噪声

### 3500 m 切换

```python
should_switch_goal(position) :=  ‖position − (0,0,0)‖ ≥ 3500
```

**每架独立判断，参考原点**（这与主办方确认的口径一致）。切换是单向的
（`goal_switched` 标志一旦置位不回退），切换后触发该机的全局引导重规划——这一步
不带空域分区（见第二节"只作用于巡航段"），直接规划到新终点。

### 动态终点

```
观测终点 = 基准终点 + 1 Hz 正弦分量 + 分段随机误差
再经一阶滤波（α = 0.5）+ 死区（1 m）
```

滤波和死区是工程加的：终点每秒抖一下，如果规划器照单全收会跟着抖，把平滑性和
能耗都做坏。死区 1 m 意味着抖动幅度小于 1 m 时直接忽略。

### 密集终点：到达球与捕获速度

表3 侧向组和后向组的相邻终点只差 **5 m**，而机间安全距离是 3 m。当前配置：

```python
# subject3_config.py
GOAL_TOLERANCE_ASSUMED = 1.0
# terminal_capture_speed = 0.（run_phase6b_final_validation.py）
```

思路：把到达球从 3 m 缩到 1 m，等于给每架划了一个不会重叠的终端领域
（5 m 间距 − 2×1 m 半径 = 3 m 净空，正好等于安全距离）；捕获速度归零避免高速
掠过邻机。

> **已排除的调整**：曾尝试把 `GOAL_TOLERANCE_ASSUMED` 收紧到 0.9 甚至 0.5，
> 期望挤出更多终端净空。实测**完全没有改善** CSV 场景的原始失败（同一处
> 卡住——那次失败发生在飞行早期，根本没到终端阶段），反而把硬编码基准的
> 614 周期成功变成了失败（0.9 → 531 周期失败）。已确认这个常量不是杠杆，
> 已改回 1.0，不要重复这个尝试。真正的解法是第二节的空域分区。

---

## 六、控制器

严格说这套系统**没有独立的控制器**，它是一个纯轨迹规划器。执行方式是：把选中
基元的前 0.2 s 前缀按 0.01 s 采样直接推进状态。控制层的职责由三处替代：

| 控制职责 | 由谁承担 |
|---|---|
| 轨迹跟踪 | 转弯预瞄跟踪器（L1）把引导航路转成每周期的前瞄目标 |
| 动力学限幅 | 静态筛选（L2）逐点校验速度/高度/过载，不满足的候选直接剔除 |
| 末端捕获 | 终端策略（L2）减速停靠 + 零捕获速度 |
| 避碰 | 集群协调（L3）时间索引预约 |

初赛那版有独立的 SCA 局部避碰层（速度障碍法 + KD 树邻域检索），**这一版被删掉
了**。取舍是合理的：复赛机间安全距离从 10 m 放宽到 3 m，而集群协调层已经在规划
阶段保证了间距，再叠一层反应式避碰反而会和规划打架。

---

## 七、模块化封装与评分接口

复赛要求"接口 + 独立指标脚本"两件独立的产物，现状：

### 输入接口：CSV 配置

`config/` 下 4 张表——`obstacles.csv`（题目表1）、`start_points.csv`、
`goal_points_initial.csv`（题目表2）、`goal_points_changed.csv`（题目表3）。
`config_loader.py` 负责解析，`mamp/envs/subject3_environment.py` 的
`load_obstacles_from_config()` / `Subject3Environment(starts_csv=..., ...)`
负责加载进规划器。评委只需要替换这 4 个文件。

### 规划入口：`run_with_config.py`

标准化命令行入口，读 CSV → 调用 `formal_case()` 跑一次 → 把结果（含 5 项官方
指标所需的全部原始数据）序列化成 `trajectory_output.json`。`formal_case()` 本身
定义在 `run_phase6b_final_validation.py`，是场景工厂函数（组装环境/规划器/
协调器，也是第二节空域分区的落地处），该文件自己的 `main()` 另有一套开发者
自用的批量回归测试，不产出比赛 JSON。`animate_phase6b_final_validation.py`
同步支持 `--config-dir`，可以对同一份 CSV 场景生成可视化动画，两条链路吃同一份
输入、互相印证。

### 评分脚本：`evaluate_metrics.py`

与规划器完全解耦的独立程序，只读 `trajectory_output.json`，按官方权重
（单次规划时长30% / 抵达时长30% / 能耗20% / 安全性10% / 平滑性10%）算出加权
总分。每项的满分/零分折算基准题面未给出官方数值，写在 `SCORE_REFERENCES`
常量里并逐条注明依据，其中能耗和平滑性的基准已用一次完整成功任务的实测值
（第〇节的 625.9 / 1.104）标定，不是凭空取值。

### 能耗与平滑性：`mission_metrics.py`

这两项此前完全无法计算——`FinalValidationEvaluator.run()` 原本只保留每个
0.2 秒周期边界的一个状态点，细粒度采样算完就丢。现在额外保留每周期内
`EXECUTION_RECORD_DT` 分辨率的完整轨迹（`segment_velocities` /
`segment_accelerations` / `segment_times`），`mission_metrics.py` 对其积分：
能耗 = Σ∫|需用过载|dt（过载定义与 L2 静态筛选用的重力常数一致），平滑性 =
∫κ²ds（评分取 24 机均值，总和另存供参考——题面未注明是均值还是总和，均值是
本版假设）。

### 过程中修复的两个真实 bug

- **接口签名不匹配**：`run_with_config.py` 早期调用 `formal_case()` 传 CSV
  路径参数，但 `formal_case()` 当时不接受这些参数，直接抛 `TypeError`——CSV
  驱动的起降点根本没有到达过规划器。已合并修复为单一权威实现，删除了一份
  行为分叉的重复文件。
- **成功路径的 JSON 序列化崩溃**：`FinalValidationEvaluator.run()` 的
  `success` 字段由 `np.all(arrival)` 算出、是 `np.bool_` 类型，
  `json.dump` 无法序列化。这个分支恰好是"24 架全部到达、提前结束"的
  **成功路径**——也就是说，比赛当天真正跑通的时候反而会写 JSON 崩溃。之前
  测试用例要么失败、要么跑满周期数，从未触发过这个分支。已改为 `bool()`
  强转。

---

## 八、对照五项考核指标的现状

| # | 指标 | 权重 | 现状 | 折算得分 | 评价 |
|---|---|---|---|---|---|
| 1 | 单次规划时长 | 30% | 24 条完整路径 **~5.4~8.2 s**（硬件相关） | 62.3 | ⚠️ 架构口径问题未解决，见问题1 |
| 2 | 无人机抵达时长 | 30% | **116.60 s**（较上版 122.80 s 缩短 6.2 s） | 93.1 | 空域分区的副产物改善 |
| 3 | 集群总能量消耗 | 20% | **625.9**（Σ∫\|需用过载\|dt） | 62.3 | 已可计算，基准取自实测 |
| 4 | 路径安全性 | 10% | 机间 **3.010~3.011 m** / 离障 2.355 m | 100 | 通过；机间裕度仍薄，见问题3 |
| 5 | 路径平滑性 | 10% | **1.104**（较上版 1.874 改善 41%） | 86.6 | 已可计算 |
| | **加权总分** | | | **77.8** | |

得分公式与折算基准见 `evaluate_metrics.py` 的 `SCORE_REFERENCES`——题面未给出
官方基准，这仍是工程假设，供内部自查趋势，不代表比赛真实评分曲线。

### ⚠️ 问题 1：指标 1 的口径（唯一未解决的大项）

主办方已确认"单次规划时长 = 一次产出所有无人机路径的用时"。这一版产出 24 条
完整路径（含空域分区的插值和几何校验）需要 **约 5.4~8.2 秒**（因机器而异，
分区前后基本不变——分区没有增加 A\* 调用次数）。作为对比，初赛 HFSP 那套一次
产出全部 24 条完整轨迹（含 B 样条优化和 TOPP）只要 **203 ms**——差 **约 30~40
倍**。滚动时域架构的单周期耗时不能拿来报这一项，因为它产出的是 3 秒轨迹片段，
不是"所有无人机路径"。**这是 30% 的分，空域分区没有触及这个问题，仍然悬着。**

### ✅ 问题 2：评估器缺失——已解决

上一版删除评估器后能耗和平滑性算不出来；现在两项都能算，见第七节。

### 🔶 问题 3：安全裕度——从"1 毫米"改善到"1 厘米量级"，但仍然偏薄

上一版机间最小距离精确卡在 3.001 m，裕度 1 毫米，任何输入扰动都可能翻车（已
实测验证：几毫米的 CSV 舍入误差就能导致失败）。空域分区后实测 3.010~3.011 m，
两种输入路径收敛一致——**不再对输入精度敏感**，但绝对裕度仍只有 1 厘米量级。
根因在第二节已经说清楚：横向走廊在低空基本不生效（所有航路挤在同一条窄缝里），
真正生效的分离手段是高度，而高度分层无法覆盖终端阶段（题面固定了终点的
y/z）。如果要进一步拉开终端裕度，`GOAL_TOLERANCE_ASSUMED` 已确认不是有效杠杆
（见第五节"已排除的调整"），需要另找机制，本版未解决。

### ⚠️ 问题 4：NumPy 2.x 不兼容

`primitive_selector.py`、`swarm_base_batch_planner.py`、`single_uav_rolling.py`
三处仍用 `np.trapz`（`mission_metrics.py` 已改用同名但显式注明版本要求的写法，
仅覆盖新增代码）。`np.trapz` 在 NumPy 2.0 已删除，主办方环境若是新 numpy，
**代码直接报错**。本版未处理。

### 其他未处理项

- 障碍运动幅值与题面图 2 不符（10 号应为 ±300 m 沿 x，代码里是 ±150 m）
- `envs/subject3_environment.py` 的 `should_switch_goals()` 仍抛
  NotImplementedError（不影响运行，`GoalManager` 里有自己的实现，属于死代码）
- 障碍表结构仍是硬编码 + CSV 双轨（CSV 覆盖全局变量），主办方"增加障碍物
  数量"这类结构性变化仍需要改代码而非纯换数据

---

## 九、结论与建议

**这一版解决了此前悬而未决的两个大问题**：能耗/平滑性从"无法计算"变成
"已计算并标定基准"；机间安全裕度的根因从"不知道为什么卡在 3.001 m"变成
"定位到 24 机航路重合，用空域分区从根上分离，副带缩短任务时间、改善平滑性"。
两种输入路径（评委的 CSV、内部硬编码默认值）现在收敛到同一结果，意味着评委
换自己的数据跑，结果的可预期性比上一版好得多。

**仍然悬着的，按优先级**：

1. **指标 1（30%）的架构口径问题完全没动。** 这不是调参能解决的——滚动时域
   天生不产出"一次性全部完整路径"，9.588 秒（现在约 5.4~8.2 秒）量级和初赛
   HFSP 的 203 毫秒差 30~40 倍。两条路径依然是：接回初赛 B 样条批量优化 +
   TOPP 做"一次产出全路径"（工作量大，收益 30%），或者把全局引导本身优化到
   1 秒级（共享搜索图、批量 A\*、多起点单次搜索，收益小风险低）。距提交截止
   时间需要重新评估是否还有空间做这件事。
2. **NumPy 2 兼容垫片**：半小时量级的工作量，但主办方环境若是新 numpy 会直接
   报错翻车，优先级应该高于问题 1。
3. **终端安全裕度仍是 1 厘米量级**，没有实质性余量。已确认到达球半径不是
   有效杠杆（第五节），需要另找机制（比如终端阶段的机间预留邻域硬约束），
   本版未展开。
4. 障碍幅值修正——小工作量，低风险，可顺手做。
