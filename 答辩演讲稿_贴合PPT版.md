# 决赛答辩演讲稿（贴合25页PPT）

> 正文约12分钟，可裁剪至10分钟（标注【可删】处）。
> 每节标注对应PPT页码，方便对稿翻页。

---

## 开场 · 封面页（P1）

各位老师、评委好。我们是北航穹卫反无集群协同规划组，我代表团队汇报
《面向低空反无的分层高效集群动态拦截》。团队成员常耀宇、闵晨、雷阳琦、张宇珺，
指导老师全权教授。

---

## Part 1 · 研究背景与任务需求（P3–P5，约1.5分钟）

**【P4 研究背景】**

先说清楚这件事的现实背景。低空空域正在被快速填满——一方面是低空经济带来的
物流、巡检无人机密度激增；另一方面，从近年的多场地区冲突可以看到，
**低成本无人机集群已经成为一种规模化的威胁手段**，单架拦截成本远高于来袭成本，
靠一对一防御在经济上不可持续。

这就是反无人机这个命题的由来：**要对抗集群，你自己必须先有能力指挥集群。**

**【P5 任务需求】**

我们调研了当前代表性的集群规划算法——MADER、EGO-Swarm、Primitive-Swarm。
它们各有所长，但本场景的需求是四个叠加：**大规模集群、密集障碍物、
动态目标点与障碍物、实时快速规划**。

这四个需求同时出现时，现有方法都会遇到各自的瓶颈。这正是我们工作的出发点。

---

## Part 2 · 初赛静态规划（P6–P10，约2分钟）

**【P7 任务场景】**

初赛是纯静态场景：24架无人机从 x=0、z 在 ±20米内均匀分布起飞，
穿越复杂障碍区，飞向约7公里外的24个拦截点，完成一对一任务分配。
障碍环境是1处圆柱体加2处长方体，形成连续绕障场景。

**【P8 方案设计】**

主流路线有三类，各有硬伤：**采样类**如RRT\*、PRM，高维适应性强但结果随机、
路径不平滑；**搜索类**如A\*、D\*Lite，结果稳定可复现但高分辨率下计算量大；
**优化与势场类**，能直接加入运动约束但易陷局部解、规模扩展困难。

我们的方案是五级流水线：**解析环境建模 → 分层拓扑搜索 → 任务指派与分流
→ B样条联合优化 → 时间参数化与解冲突**。

这里想强调一点：**任务指派与分流这一级，是专门为"集群"设的。**
它优化目标匹配、减少轨迹交叉——**24条单独最优的路径，放在一起不一定可行**，
这个认识在决赛被彻底验证了。

**【P9–P10 仿真结果】**

初赛结果：集群完工237.58秒，最长单机航程7.10公里。
在线规划总计**203.02毫秒**，单机热启动8.60毫秒。

约束验证全部通过且留有余量——最大速度30.00 m/s（限30）、最大高度77.92米（限100）、
水平合加速度1.702g（限2g）、**最小离障27.88米（要求12米）、最小机间13.17米（要求10米）**。
我们还按约1米间隔独立复核了24条轨迹的离障安全与端点检查。

初赛给了我们两个判断，直接决定了决赛的技术路线：
**第一，可复现性对竞赛评测极其关键**，随机性强的方法在评委复跑时是巨大风险；
**第二，单纯追求单条路径最优，对集群场景没有意义。**

---

## Part 3 · 决赛动态规划方案升级（P11–P18，约4.5分钟，本节为重点）

**【P12 决赛场景】**

决赛把三件事同时变成动态的。

起飞编队展宽到 z∈[-40,40]，初始目标在 x=5000。约束更严——
**速度放宽到50 m/s，但机间安全距离从10米压到3米、离障从12米压到1.5米。**

障碍环境是**16个静态加3个动态**。而最关键的变化是动态任务：
**3500米触发目标切换，终点重组为三组，且伴随动态障碍与目标扰动。**

**【P13 初赛→决赛】**

这一页是三个升级维度：大规模、实时性、安全性。

我想特别说明其中一个数字，避免误读。**初赛的203毫秒和决赛的5.36秒，
不是同一个指标**：

- 初赛203毫秒是**在线滚动规划**的单次耗时；
- 决赛5.36秒是**一次性产出全部24条完整全局路径**，这是组委会明确定义的"单次规划时长"；
- **决赛同口径的滚动周期耗时是177毫秒——比初赛的203毫秒还快**，
  而且还多处理了动态障碍物预测和动态目标。

**【P14–P15 算法流程与技术路线】**

我们的系统按**时间尺度**分三层：**L1全局引导层**离线一次，
**L2局部轨迹规划层**每0.2秒一轮，**L3集群协调执行层**同样每0.2秒。

整体形成"全局引导—局部生成—安全筛选—集群协调—同步执行"的滚动闭环。

为什么分层？因为**没有任何单一方法能同时满足实时性、硬安全保证和可复现**。
分层让每一层只解决它擅长的问题。

**【P16 L1：从"算路径"到"分空域"】**

L1的基础是稀疏网格上的加权A\*，工程上做了两处加速：
**预计算边有效性查找表**，把邻域每条边是否可通行提前算好，搜索时在线查表；
**基于真实几何的视线剪枝**，把A\*产生的锯齿状路径拉直。

但真正的创新，是我们在压力测试后才补上的。

**这里要讲一个我们踩过的坑**：同样的算法、同样的场景，仅坐标精度差几毫米，
结果就从成功翻转成失败。我们没有当数值问题加容差了事，而是深挖了根因——

**L1的代价函数是纯欧氏距离，对其他无人机完全无感知。**
24架机起终点高度相似，算出来的24条航路几乎重合，**机间距仅3.478米，
而安全红线是3.0米**。这是一个零裕度系统，任何毫米级扰动都能把它推过阈值。

这个诊断把问题从"调参数"提升到了"架构缺陷"——**必须在空域层面把飞机分开。**
于是有了V2的空域分层：

- **高度分层**：24架机交错分配到多个高度层，**层间距必须覆盖L2局部垂直修正的范围**，
  这不是调出来的而是推出来的。**实测跨层冲突为0。**
- **横向走廊**：巡航段叠加多档横向偏移，基于真实障碍几何逐级校验，
  **不可行则自动缩小或回退**——这是失败自适应的降级阶梯，不是写死的固定方案。

**【P17 L2：非反应式，而是预测式】**

L2根据当前位置、速度、加速度生成五次多项式候选轨迹。
选五次多项式是因为它能**解析地满足位置、速度、加速度三组边界条件**，
轨迹天然连续可导，无需额外平滑。

候选依次通过三道关卡：动力学约束、静态障碍、动态障碍。

**这一页的标题是"非反应式，而是预测式"，这是我们相对ORCA类方法的本质区别。**
3个动态障碍物是旋转长方体，不仅平移还在自转。我们不是拿它当前位置判断碰撞，
而是**预测它在未来整个时域内每个时刻的中心位置和偏航角**，再逐时刻校验。
**我们避的是它几秒后会在哪，不是它现在在哪。**

**【P18 L3：动态优先级 + 一步前瞻】**

L3解决"谁让谁"。经典的优先级规划给每架机固定优先级，会导致低优先级的机
被持续饿死。**我们的优先级每周期重算**，按候选裕度、冲突度与安全裕度排序——
**谁的可行候选最少，谁先选**。含义很直观：把选择权优先交给最被逼到墙角的那架机。

**一步前瞻**是针对死锁的：一条轨迹"此刻安全"不等于"能继续飞"。
我们做一次影子推演——假设选了它，下一周期还有没有可行解？**没有就地否决。**

而大规模机间避障上，我们**避免全局联合优化**，把组合搜索限制在局部冲突簇内，
抑制复杂度随集群规模快速增长。

---

## Part 4 · 仿真验证（P19–P22，约2.5分钟）

**【P20 仿真结果】**

24机协同规划，约5.4秒生成规划结果，24条轨迹通过独立安全复核：
**24/24全部抵达，任务时长116.60秒，零碰撞，集群总能耗626.920，路径平滑性1.149961。**

**【P21 评分分析】**

由于题面只给了五项指标的权重、没有给满分基准，我们自己定义了归一化方法：
对"越小越好"的指标做线性插值加双端截断，再按30/30/20/10/10加权。

请注意路径安全这一行：**最小机间距3.011米、最小静态障碍物距离2.355米、
最小动态障碍物距离102.336米，全部达标，零碰撞零违规。**

路径平滑性这一项需要说明方向：它是曲率平方沿路径的积分，**数值越小越平滑**，
直线的曲率为零。

**这里我想坦率说明一点**：这些基准是我们的工程假设，不是官方数值。
比如规划时长我们把满分线设在接近实时的1秒——**这是一个对自己苛刻的标准**。
我们把"哪些是官方口径、哪些是工程假设"在代码注释里逐条标注了，
评委如有更准确的基准，只需改一个常量，评分逻辑一行不用动。

**【P22 场景配置接口】**

按组委会的模块化封装要求，我们把**全部输入外置成五类CSV**：
障碍物、动态运动参数、起点、初始终点、切换后终点。
**规划脚本与评分脚本完全解耦**——评分脚本不import任何规划代码，只读JSON。

更重要的是，我们没有只在官方数据上验证，而是**主动设计变换场景寻找失效边界**：
目标重排、起点压缩、新增障碍、目标扩展。

测试确实暴露了真实问题，我举一个：**起点压缩这一组失败了，但它不是代码缺陷**——
题面给定的起点间距3.478米，已经只有3米安全线的116%余量，压缩一半后
飞机起飞前的前置校验就直接拦下。**这说明起点密度已经没有压缩空间，
是题面数据本身的边界。**

【可删】另一组"新增障碍物"的失败点，经诊断精确定位在**终点切换区域**
而非新增障碍物附近——这个反直觉的结果让我们意识到，
**拥堵的根源是"24架机同时扇形散开"这个动作，而不是某一处几何窄道。**

---

## Part 5 · 创新点与总结（P23–P25，约1分钟）

**【P24 四项核心创新】**

四项创新对应三层架构：

**结构化空域分区规划**——把多机冲突消解从"在线避让"**前移**到"离线空域预分配"；

**分层滚动运动基元**——五次多项式候选加三级筛选，**预测式而非反应式**；

**轻量化集群时空协同**——动态优先级破解优先级饥饿，一步前瞻破解集群死锁；

**动态任务闭环规划**——目标切换触发全局重规划，**滤波而非跟随**抑制目标抖动，
终端减速捕获实现动态目标收敛。

**【P25 结束】**

最后我想说，这次工作最有价值的不是某一组能跑通的参数，
而是建立了一套**诊断—定位—验证**的闭环：每次改动都配套可复现的量化诊断，
精确到"第几周期失败、哪两架机冲突、冲突发生在哪个坐标"；
每个假设都用实测去证伪，**包括推翻我们自己提出的假设**。

汇报完毕，恳请各位老师批评指正，谢谢。

---
---

# 5分钟问答准备

## 一类：方法选择

**Q1：为什么不直接用CBS或ORCA这些成熟方案？**

CBS完备性好但24机规模下冲突树指数膨胀，实时性没保障；ORCA快但纯反应式、
不看未来，且在对称场景下极易死锁——我们这个题目起终点高度对称，正是它的最坏情况。
我们的L3本质上是取两者之长：**用动态优先级保证实时性，用一步前瞻补上ORCA缺失的未来信息。**

**Q2：初赛用B样条优化，决赛为什么改成多项式基元？**

场景性质变了。初赛是静态的，可以离线做全局联合优化，B样条在平滑性上有优势。
决赛有动态障碍和跳变目标，**必须每0.2秒重规划一次**，此时需要的是
能解析满足边界条件、可以大批量快速生成和筛选的候选族。五次多项式正好满足这个要求。

**Q3：为什么不直接加大安全容差？**

容差是被动兜底，治标不治本。病根是L1对"还有23架同类飞机"完全无感知。
我们实测过把到达容差从1.0降到0.9和0.5，**两次都让基准场景退化**，这条路走不通。

## 二类：参数与设计细节

**Q4：高度分层的层间距怎么定的？**

不是调出来的，是**推出来的**——必须大于L2局部垂直修正范围的两倍，
否则下层的避让动作会穿进相邻层。我们试过更小的间距，直接退化到提前失败，
反向印证了这个约束。

**Q5：为什么横向走廊要做降级阶梯，不能固定一个偏移量？**

因为**横向平移不像抬高那样必然安全**。抬高是安全的——障碍物是地面锚定的棱柱，
横向无碰的路径抬高后仍然无碰。但横向平移可能撞上远离中线的障碍物，
所以必须逐档尝试、每档真几何校验、不行就降级。

**Q6：动态目标滤波会不会导致跟踪滞后？**

会有滞后，但这是必须的取舍。**目标抖动幅度是到达判定球半径的3倍**——
如果直接跟随抖动，飞机会持续追逐晃动，**永远无法稳定判定"到达"**。
死区设置与到达容差同量级，使小于判据的抖动被直接忽略，大于判据的变化仍然响应。

## 三类：指标与结果

**Q7：评分基准是你们自己定的，会不会偏向自己？**

会有这个风险，所以我们做了三件事：第一，基准由**一次完整成功任务的实测值定标**，
让基准运行落在评分区间中段而非贴着满分；第二，在代码注释里
**逐条标注哪些是官方口径、哪些是工程假设**；第三，这个分数不是比赛成绩，
评委会用自己的基准——我们真正拿得出手的是原始实测值：**24/24到达、零违规、
机间距3.011米**。

**Q8：单次规划时长5.36秒是不是偏慢？**

需要区分两个口径。5.36秒是**一次性产出全部24条完整全局路径**的离线过程；
**在线滚动周期的耗时是177毫秒，比初赛的203毫秒还快**。
我们也量到了进一步优化空间：把长距离单段规划拆成两段拼接，
**耗时反而下降**，因为A\*耗时对距离是超线性的。这是下一步的直接优化方向。

## 四类：鲁棒性（⚠️ 重点准备）

**Q9：如果评委用不同的障碍物运动参数测试，能不能跑通？**

这是我们目前最关注的风险点，坦率汇报我们的实测结论。

我们做过一组极端测试：**把动态障碍物的周期缩短10倍、振幅不变**，
此时最快的障碍物速度达到**314 m/s，是无人机最大速度的6.3倍**。
这组测试**没有跑通**，我们定位到了确切原因——

其中一个障碍物振幅500米、本体200×40米，它在横向的**扫掠带宽达到1135米**，
而我们的航路恰好从扫掠带正中穿过。诊断显示9架机的候选轨迹被全部清空，
**候选的最小动态净空中位数是0.00米**，也就是说它们直接从障碍物体内穿过——
L2的局部横向权限对上千米的扫掠带是无意义的。

**更重要的是，我们发现原本"成功"的那组也是相位运气**：
它的振幅完全一样、包络完全一样，只是机队经过时障碍物恰好在扫掠带的另一端。
换个初相位同样会失败。

**所以这不是"障碍物变快才出问题"，而是我们的方案对扫掠包络从来就没有防御。**

修复方向已经明确并验证了可行性：**在L1建栅格时把动态障碍物的全周期扫掠包络
当作静态障碍物一并标记**，让A\*从源头绕开。我们已经算过，
扣除全部包络后仍然存在贯通的安全通道，**解是存在的**。
这个改动能彻底消除相位依赖。

> **答辩策略提示**：这个问题如果评委不问，主动讲的性价比不高（PPT里没有这一页）。
> 但一旦被问到鲁棒性、泛化性、或者评委自己换数据测出了问题，
> **务必按上面这套讲**——承认失败、给出精确诊断、说明修复方向已验证可行。
> 最忌讳含糊其辞说"应该没问题"。

**Q10：泛化性测试里有失败场景，怎么看待？**

暴露问题比掩盖问题有价值。失败精确定位了两个薄弱点：
分配规则对数据顺序的隐含假设、以及终点切换区的拥堵机制。
如果我们只跑官方那一组数据，这两个问题会一直藏到评委复跑时才爆发。

**Q11：方案能推广到不同机队规模吗？**

分层和分组逻辑都是从数据算出来的，代码里没有写死机队数量。
但我要诚实说明：**完整验证主要在24机、19个障碍物这组数据上**，
规模大幅变化后的表现还需要进一步测试，这一点我们不回避。
