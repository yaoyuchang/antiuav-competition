# 评分体系设计说明

> 本文说明复赛科目3官方5项评分指标如何映射到代码实现，供撰写报告、答辩PPT时引用。
> 对应代码：`code/run_example/evaluate_metrics.py`（评分脚本）、
> `code/run_example/mamp/simulation/mission_metrics.py`（能耗与平滑性积分）。

---

## 一、设计出发点：两个硬约束

### 约束1 —— 题面的模块化封装要求

题面要求规划器与评分脚本必须是两个独立程序。

代码落实：**`evaluate_metrics.py` 不 import `mamp/` 下任何规划代码**，只读取
`trajectory_output.json`。这意味着评委可以拿我们的评分脚本去评别人的结果，
也可以用别人的脚本评我们的——接口是一个纯 JSON 文件，不是函数调用。

### 约束2 —— 题面只给权重、未给基准

官方公布的权重：

| 指标 | 权重 |
|---|---|
| 单次规划时长 | 30% |
| 无人机抵达时长 | 30% |
| 集群总能量消耗 | 20% |
| 路径安全性 | 10% |
| 路径平滑性 | 10% |

但题面**没有说明"多少秒算满分、多少秒算零分"**。因此归一化方法由我们自行定义，
并在代码注释中逐条标注**哪些是官方口径、哪些是工程假设**。

---

## 二、五项指标的数学定义

### 2.1 集群总能量消耗（20%）

题面定义：$\Sigma \int |需用过载| \, dt$

**"需用过载"的解读**：以重力加速度为单位的加速度幅值

$$n(t) = \frac{\lVert \mathbf{a}(t) \rVert}{g}$$

单机能耗与集群总能耗：

$$E_i = \int_{0}^{T} \frac{\lVert \mathbf{a}_i(t) \rVert}{g} \, dt,
\qquad E_{\text{swarm}} = \sum_{i=1}^{24} E_i$$

**为什么这样解读**：原语选择器在校验加速度约束时本就使用同一个 `G` 常量
（`A_XZ_MAX = 2.0*G` 等）。沿用同一常量保证了"规划时的约束口径"与
"评分时的能耗口径"完全一致，不会出现按 A 标准飞、按 B 标准评的情况。

实现：`mission_metrics.py::_integrate_uav_energy()`，梯形法数值积分。

### 2.2 路径平滑性（10%）

题面定义：$\int \kappa^2 \, ds$

曲率由速度、加速度采样计算：

$$\kappa = \frac{\lVert \mathbf{v} \times \mathbf{a} \rVert}{\lVert \mathbf{v} \rVert^3},
\qquad ds = \lVert \mathbf{v} \rVert \, dt$$

代入化简得实际积分形式：

$$S_i = \int \kappa^2 \, ds
= \int \frac{\lVert \mathbf{v} \times \mathbf{a} \rVert^2}{\lVert \mathbf{v} \rVert^5} \, dt$$

> ### ⚠️ 方向提示（写报告/PPT时务必注明）
>
> **该指标数值越小越平滑。** 直线的曲率 $\kappa = 0$，路径拐得越急越频繁，积分值越大。
>
> 但指标名"路径平滑性"字面上容易让读者误以为"数值大 = 更平滑"，**方向正好读反**。
> 建议在表格中显式标注，例如：
> `路径平滑性 ∫κ²ds（越小越平滑）` 或直接改列名为"曲率能量"。

**题面歧义的保守处理**：题面对能耗明确写了"集群**总**能量消耗"，
但"路径平滑性"未说明是24机均值还是总和。因此两者都计算、都输出：

- `mean_path_smoothness`（均值）—— **用于评分**
- `total_path_smoothness`（总和）—— 仅作参考

选均值评分的理由：总和会随机队规模线性膨胀，均值才是"单条路径质量"的度量，
与指标名称的字面含义更贴合。

实现：`mission_metrics.py::_integrate_uav_smoothness()`，
对近零速度做 $\max(\lVert \mathbf{v} \rVert, 10^{-6})$ 保护，避免悬停瞬间曲率发散。

### 2.3 单次规划时长（30%）

**口径已与组委会确认**：指一次性产出全部24条完整全局路径的墙钟时间，
**不是**滚动时域每个 0.2 秒周期的延迟。

实现：`formal_case()` 中用 `time.perf_counter()` 包住整个 L1 规划过程
（含空域分区的通道分配与航路整形）。

### 2.4 无人机抵达时长（30%）

全部24架到达各自终点的任务总时长 $T_{\text{mission}}$，等于 `完成周期数 × 0.2 秒`。

### 2.5 路径安全性（10%）

安全是红线问题而非渐变问题，故采用**阈值扣分制**而非连续插值：

$$S_{\text{safety}} = \max\Big(0,\ 100
- 30 \cdot \mathbb{1}[d_{\text{uav}} < 3.0]
- 30 \cdot \mathbb{1}[d_{\text{static}} < 1.5]
- 20 \cdot \mathbb{1}[d_{\text{dyn}} < 1.5]\Big)$$

其中 $d_{\text{uav}}$ 为全程最小机间距，$d_{\text{static}}$、$d_{\text{dyn}}$
为离静态/动态障碍物的最小距离；3.0 米与 1.5 米为题面给定的安全阈值。

---

## 三、归一化打分函数

四项"越小越好"的连续指标统一采用**线性插值 + 双端截断**：

$$\text{Score}(x) = 100 \times \text{clip}\!\left(
\frac{x_{\text{zero}} - x}{x_{\text{zero}} - x_{\text{full}}},\ 0,\ 1 \right)$$

即 $x \le x_{\text{full}}$ 记 100 分，$x \ge x_{\text{zero}}$ 记 0 分，中间线性过渡。

实现：`evaluate_metrics.py::_linear_score()`。

### 基准值取法

> **说明：下列基准为工程假设，非官方数值。** 题面未给出基准，
> 若评委或队内有更准确的依据，只需修改 `SCORE_REFERENCES` 常量，评分逻辑无需改动。

| 指标 | 满分基准 | 零分基准 | 依据 |
|---|---|---|---|
| 规划时长 | 1.0 s | 20.0 s | 工程假设，1秒≈接近实时 |
| 抵达时长 | 103 s | 300 s | 103秒取自理论下界估算（直线飞行时间量级），零分取其约3倍 |
| 能耗 | 400 | 1000 | 由一次完整成功任务的实测值定标 |
| 平滑性 | 0.5 | 5.0 | 同上 |

**能耗与平滑性的定标方法**：并非凭空取值，而是先跑通一次完整成功任务
（competition 模式、种子 0、24/24 到达），测得能耗约 617.7、平滑性均值约 1.874，
再将满分/零分取在该实测值的上下两侧，**使基准运行落在评分区间中段**，
而不是贴着 0 分或 100 分。

这样做的目的是保证评分函数对改进保持**敏感**——若基准贴着满分，
后续所有优化都会因截断而看不出差异。

### 失败清零规则

$$\text{若 success} = \text{False}:\quad
S_{\text{arrival}} = S_{\text{energy}} = S_{\text{smooth}} = 0$$

**理由**：任务未完成时轨迹是残缺的，此时算出的"能耗低""时间短"
反而是因为飞得少，属于**虚假的好成绩**，必须清零。
规划时长与安全性仍照常计分，因为它们对残缺轨迹仍有意义。

---

## 四、加权总分

$$\text{Total} = 0.30\,S_{\text{plan}} + 0.30\,S_{\text{arrival}}
+ 0.20\,S_{\text{energy}} + 0.10\,S_{\text{safety}} + 0.10\,S_{\text{smooth}}$$

---

## 五、支撑性工作：细粒度采样

能耗与平滑性**在早期是算不出来的**——评估器原本只记录每个 0.2 秒周期
**边界**上的状态快照，这种粗粒度采样做数值积分误差极大。

改动：`FinalValidationEvaluator.run()` 在每条周期日志中额外保留整个执行段的
细粒度采样（`segment_positions` / `segment_velocities` /
`segment_accelerations` / `segment_times`，按 `EXECUTION_RECORD_DT` 分辨率）。

**先有可积分的数据，这两项指标才成立。**

---

## 六、实测评分结果

| 指标 | 3层分层方案 | 4层分层方案 | 权重 |
|---|---|---|---|
| 完成周期 / 到达 | 583 / 24-24 | 585 / 24-24 | — |
| 任务时长 | 116.60 s | 117.00 s | 30% |
| 单次规划时长 | 8.956 s | 7.213 s | 30% |
| 集群总能耗 | 626.920 | **594.470** ↓5.2% | 20% |
| 路径平滑性 ∫κ²ds（越小越平滑） | 1.1500 | **0.6774** ↓41% | 10% |
| 最小机间距 | 3.011 m | **3.020 m** | 10% |
| **加权总分** | **76.4** | **81.2** | — |

4层方案各分项得分：

| 分项 | 得分 | 权重 |
|---|---|---|
| 单次规划时长 | 67.3 | 30% |
| 无人机抵达时长 | 92.9 | 30% |
| 集群总能量消耗 | 67.6 | 20% |
| 路径安全性 | 100.0 | 10% |
| 路径平滑性 | 96.1 | 10% |

> **需在报告中如实说明**：单次规划时长是墙钟时间，受机器负载影响。
> 上表两次测量的机器负载不完全一致，该项差异不宜全部归因于方案本身。
> 能耗与平滑性由轨迹积分得出，不受负载影响，是确定性的改进。

---

## 七、四个可作为亮点的设计取舍

1. **评分脚本零依赖规划代码** —— 真正满足模块化封装要求，
   而非形式上拆成两个文件。
2. **口径一致性** —— 能耗使用的重力常量与规划器约束校验使用的是同一个，
   不存在"按 A 标准飞、按 B 标准评"。
3. **歧义处理透明化** —— 题面对平滑性是均值还是总和未说明，
   我们两者都算、并标注清楚用哪个评分，而非默默选一个对自己有利的。
4. **基准由实测定标，而非凭空取值** —— 并在代码注释中逐条标注
   "官方口径 vs 工程假设"；基准需调整时只改 `SCORE_REFERENCES` 常量，
   评分逻辑一行不动。

---

## 八、代码位置索引

| 内容 | 位置 |
|---|---|
| 评分脚本主入口 | `code/run_example/evaluate_metrics.py` |
| 基准常量 | 同上，`SCORE_REFERENCES` |
| 官方权重 | 同上，`OFFICIAL_WEIGHTS` |
| 归一化函数 | 同上，`_linear_score()` |
| 能耗积分 | `code/run_example/mamp/simulation/mission_metrics.py::_integrate_uav_energy()` |
| 平滑性积分 | 同上，`_integrate_uav_smoothness()` |
| 细粒度采样记录 | `code/run_example/mamp/simulation/final_validation_evaluator.py::run()` |
| 规划时长计时 | `code/run_example/run_phase6b_final_validation.py::formal_case()` |

### 使用方式

```bash
cd code/run_example
python run_with_config.py --config-dir ../config --mode competition \
    --cycles 2000 --output /tmp/result.json --seed 0
python evaluate_metrics.py --input /tmp/result.json
```
