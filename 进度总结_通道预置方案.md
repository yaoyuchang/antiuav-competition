# 进度总结：切换点拥堵优化（通道预置方案）

## 现状

已完成并可运行、但**未验证是否达标**的半成品代码，改动如下：

1. **新增 `code/run_example/mamp/planners/lane_assignment.py`**
   - `free_lanes()`：查询某个(x,高度)切片的自由横向区间（纯几何，复用
     `points_are_static_safe`）。
   - `group_by_effective_goal()`：按"有效终点z"（切换后终点，未启用切换时
     回退首段终点）把24架机聚类分组，按数据自动分组、不写死组数。
   - `assign_lane_offsets()`：给每组分配一个目标通道中心（默认=该组自己
     的有效终点z），做可达性校验，不可达则向中线收缩重试。

2. **改 `mamp/planners/route_shaping.py` 的 `shaped_initial_route()`**
   新增 `lane_offset` 参数，启用时按折扣阶梯(1.0/0.7/0.4)尝试该偏移，
   全部失败退回原有 `GUIDE_CORRIDOR_SCALES` 逻辑（用
   `GUIDE_LANE_ASSIGNMENT_ENABLED` 开关一键回退到已验证的583周期版本）。

3. **改 `run_phase6b_final_validation.py` 的 `formal_case()`**
   先规划全部24条guide路径，再统一调用 `assign_lane_offsets()`，
   最后逐架应用 `shaped_initial_route()`。

4. **`subject3_config.py`** 新增 `GUIDE_LANE_*` 系列常量，注释里记录了
   本节下面的实测数据。

## 发现的问题：简单直线平移在物理上对多数机不成立

用真实A*障碍物几何验证后发现：**把巡航段z直接平移到目标终点z（南组
z≈-560、北组z≈+560）这个"一条直线斜着切过去"的手段，只对南组可行**：

| 组 | 目标 | 拓宽爬升比例阶梯(0.12/0.20/0.30)后的可行率 |
|---|---|---|
| 南组(8-15号, z≈-540~-575) | **8/8 可行** |
| 中组(0-7号, z≈-30~-100) | 仅 2/8 可行 |
| 北组(16-23号, z≈+540~+575) | 仅 1/8 可行 |

**根因**：中组/北组的直线路径上有5个障碍物(2/5/7/12/13/14号，高度
70~95米)，其中4个比90米顶层还高，任何高度层、任何爬升比例都绕不开——
这是几何上真实存在的墙，不是"折扣阶梯不够细"能解决的。

## 已验证的更优方案，尚未实现

与其做人工直线平移，改成**复用现有A*规划"起点→通道内经停点→原终点"
两段路径**，让障碍物绕行交给A*本身处理（这正是A*本来擅长的事）。

**关键发现：这个方案不仅更可靠，实测还更快**：
```
单段A* × 24架：8.424秒
两段A* × 24架：4.878秒（约0.6倍，不是预期的2倍——A*耗时对距离非线性，
                        两段变短的规划反而比一段长的更快）
```
这意味着"单次规划时长"（官方指标30%权重）不但不会变差，可能还会变好。

## 下一步计划（尚未开始实现）

1. 在 `formal_case()` 里，对每架机改成两段A*拼接：
   `guide_planner.plan(start, 通道经停点)` + `guide_planner.plan(通道经停点, 原终点)`，
   经停点取 `[某个x, 该机的高度层, assign_lane_offsets算出的目标通道z]`。
2. 三组（南/中/北）统一用这个方法，不再区分"能不能简单平移"——
   南组虽然简单平移已可行，但统一用两段A*架构更简单、不用维护两套代码路径。
3. 拼接后的路径再套用现有的 `band_waypoints()` 做高度爬升整形（如果还需要）。
4. **完整回归**（尚未做，是最终验收标准）：
   - 基准门槛 `--config-dir /nonexistent_force_defaults`：需 ≥583周期、成功、24/24
   - CSV门槛 `--config-dir ../config`：需 ≥583周期、成功、24/24
   - 切换区拥堵复测（`switch_zone_diag.py`）：目标是把 x∈[3200,3600) 的
     `<6m`/`<4m` 事件数（当前3层285/61、4层313/101）显著压下去
   - 通道使用情况核查（`lane_usage.py`，已写好但会因为上面的直线平移
     问题而显示大量"alt-only"，等两段A*方案实现后需重新核实）
   - `config_tests/` 5组泛化性测试（尤其01/04两组）
   - 层数（3层 vs 4层）的最终取舍，等两段A*方案落地、重新测过拥堵数据后再定

## 已验证但还没跑通的诊断脚本（在scratchpad，未纳入仓库）

- `switch_zone_diag.py`：切换区同层/跨层近距离事件拆分（已证实3层和4层
  都是100%同层冲突，跨层0%——高度分层本身没问题，矛盾全在横向）
- `congestion_predictor.py` / `congestion_predictor2.py`：两版纯几何拥堵
  预测器，均验证失败（热点是"切换事件"造成的，不在首段几何里，这也是
  为什么放弃"找热点"改成"按去向预置通道"的直接依据）
- `lane_usage.py`：每架机最终用了哪条通道/降到第几级（当前会显示问题，
  等两段A*方案实现后重新核实）

## 当前代码状态

`lane_assignment.py`/`route_shaping.py`/`formal_case()`/`subject3_config.py`
的改动都在工作区里、可以正常import和跑通，**但还没有跑过上面第4条的
完整回归**，不确定是否达到583周期基准。`GUIDE_LANE_ASSIGNMENT_ENABLED=True`
是当前默认值，如果需要先保证有一个能跑的版本，可以先设为 `False`
退回到已验证的纯走廊版本（583周期/76.4分）。
